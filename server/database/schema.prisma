generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// MODELS
model User {
  uid  String @id @default(uuid())
  name String @unique

  @@map("users")
}
